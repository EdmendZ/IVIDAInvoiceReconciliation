# Harness 启动检查

> 历史记录：下文旧版隔离前提已由文末用户修正及 IR-SIMPLE-1.0.1 取代，不再是开发阻塞。

## 用户原话

“继续”——承接 Spec 交付，继续推进实施准备。

## 已完成检查

- 核对 Spec 冻结要求、任务清单及当前 Git 工作区。
- baseline.json 中 290 个文件 SHA-256 与当前文件一致，没有基线漂移。
- 本次核对的 Spec SHA-256：449e7484f5dde95497f0661bf34f4fc992ab76a262e9ffd5a4a20138eab42ea6。
- 中文界面与 Spec 等内容仍为未提交改动，没有 reset、clean、提交或推送。
- 本机有 docker.exe、wsl.exe 和 codex.exe；WSL 可列出 Ubuntu 与 docker-desktop。
- Docker 引擎连接失败，错误为找不到 dockerDesktopLinuxEngine 命名管道。

## 当前执行前提不足

尚无已经验证的文件级可写投影、只读 Spec/检查器、网络限制和任务结束后撤销工具权限的可信执行器。当前会话的主机工具具有广泛文件系统访问能力；新子会话不会自动获得 Spec 所要求的文件系统隔离。

依据 spec/06-harness.md 第8节，本机缺少已验证隔离时不能正式接纳任务，不允许执行 Agent 自行降低要求。因此未派发 T00，也未声称 Harness 已部署。

启动 Docker 只是环境准备的一部分，不能据此声称硬隔离已经就绪。下一步需要先落实并验证可信隔离宿主，或由用户明确批准修改 Spec 为差异检查模式；两者不能混淆。此记录不是批准/冻结签名，也不是任务通过报告。


## 用户修正与已确认决定

用户原话：“不是 怎么要我搭建docker harness是什么意思”；“修正 拒绝问题复杂化 继续开发”。

已按此修正为独立任务上下文、精确文件白名单、事后差异检查和测试验收。无需搭建 Docker/VM、配置操作系统级隔离或手工签署冻结文件。不声称上下文关闭等于销毁操作系统进程或凭据。业务仍为 Invoice / Receive Note 自动处理、自动候选、同页查看差异和一次确认，不加入合同、认领或多级审批。

## 开发进度（协调者核验）

- T00 已完成，独立运行 Harness 测试：42 通过，1 个本机符号链接权限相关跳过；其他链接/越界测试通过。
- T01 已完成，独立运行 36 个契约测试全部通过；Spec 与文件范围检查通过。
- 原有 PostgreSQL 两个集成测试已在独立临时测试数据库通过；不使用业务库进行破坏性测试。
- 后续继续依次实现纯规则、事务存储、后台自动处理、API 和中文工作台。当前不宣称新工作台已交付或部署。
