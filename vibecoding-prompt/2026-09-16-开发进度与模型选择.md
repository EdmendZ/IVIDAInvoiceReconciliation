# 发票工作台开发记录

## 用户原话

“修正 拒绝问题复杂化 继续开发。”

“继续以合适的模型继续任务 避免使用superpower开发 节省token。”

## 已确认执行方式

在原项目和现有环境直接开发，保留每任务独立上下文、文件范围检查和必要测试。不使用 Superpowers 开发。后续任务按复杂度选择模型，常规开发采用 GPT-5.6 Sol，精简交接和日志输出；已完成上下文不复用。

## 实际进度

T00 检查基座、T01 数据接口、T02 确定性匹配与比对、T03 原子存储均已验收。T03 的 57 项 PostgreSQL/契约测试无跳过；协调者另行复核了事务回滚、并发抢占、同请求重放。旧业务回归和补齐的数据字典检查通过。独立测试库迁移前后，旧版本和对账记录校验值完全一致。

Spec 当前 IR-SIMPLE-1.0.3：补齐历史导出编号、提前同步数据库字典，并给后续集成任务补上已有 CI 的修改范围。均为原需求的实现遗漏修正，不加入新业务。

接下来开发后台自动同步和预览、服务/API、中文工作台；新工作台尚未全量完成或部署。商业合同不在范围内。


## T04—T08 验收更新

- 用户原话：继续以合适的模型继续任务，避免使用 superpower 开发，节省 token。
- 已执行：全程未使用 Superpowers；T04/T05/T06/T08 主实现使用 Sol，T07 使用 Luna。T08 Sol 因额度中断，保留改动，由全新 Luna 上下文完成收尾。
- 已验收：T04 自动 Worker（18 测试）；T05 用例/上传/CSV（13 测试）；T06 HTTP 与旧写入口限制（46 指定＋59 回归）；T07 前端类型/客户端（8 测试）；T08 中文工作台/单据详情/历史只读（54 前端测试、build、范围检查通过）。
- T08 主会话复核修正：自动提取完成填充未修改字段、后到收货单持续轮询、保留未保存输入、预览变化清除确认勾选、切换单据忽略旧响应。
- 主会话已用样例检查 1440×900 与 390×844 页面；长英文、多张 RN、缺价格与差异确认按钮无横向遮挡。属于样例视觉证据，不代表真实数据库端到端或真实 TapTouch 接入。
- 已知限制：意外纯预览计算异常由 Worker 记录日志/计数；当前端口没有将此类异常持久化到 document.error_code 的操作。
- 下一步 T09：独立 PostgreSQL＋API＋Worker 集成验收、文档/CI同步；随后 T10/T11。未部署，未开启生产功能。


## 2026-09-17 最终验收与交接

- T09 已验收：7 项真实 PostgreSQL API/Worker 场景全过、零跳过；包含真实提取 Worker 与工作台 Worker 串联、先后到达、幂等确认、过期预览、重开快照保持、CSV。外部解析/模型/存储用测试替身。CI 已接入 repository 与 acceptance 两套工作台 PostgreSQL 测试。
- T10 已验收：47 项约束测试通过；1 项 Windows 创建符号链接权限跳过。H04/H05 以协调者实际执行记录核对，自动测试不依赖本机 .git 文件，不宣称有 OS 硬隔离。
- T11 已验收：完整后端 488 项中 487 通过、0 失败/错误、1 项上述平台权限跳过，PostgreSQL 零跳过；前端 54/54 通过，构建、Spec、范围、文档同步、diff 检查通过。报告见 `.harness/runs/T11/result.json`。
- 当前冻结计划 T00—T11 全部完成本地开发验收；manifest 的 not_started 是冻结初始值，真实进度以任务报告/协调验收记录为准。
- 已实现：中文工作台＋英文原始数据；Invoice/Receive Note 任意先后上传；后台自动提取、匹配、比对；一致也保留一次人工确认；差异待核实/带说明处理；未核验价格不补零；历史快照不变、旧流程只读。无商业合同模块。
- 模型：后续 T09/T10/T11 使用 Luna，未使用 Superpowers。T09 执行上下文在主会话独立测试通过后停止，由主会话补齐真实验收报告；未继续派发未通过的任务。

## 2026-09-17 第二阶段梳理

- 用户原话：请继续开发，后续开发内容是什么，和我讲讲。
- 已核对：冻结 Spec 的 T00—T12、开发入口和简化确认流程均已完成；当前不存在原任务表中的未完成业务任务。
- 已纠正：README 移除旧的认领、分派、Admin 二次审批和重复工作台说明，与当前一次确认流程保持一致。
- 助手建议：第二阶段先做可重复的英文演示数据与浏览器完整闭环，再基于真实业务样本校准抽取；真实 TapTouch 接入、正式身份/Secret、告警和生产部署分别作为后续独立任务。
- 待确认：TapTouch 真实接口资料和真实脱敏样本尚未提供，不能宣称已完成生产集成或真实准确率验证。
- 视觉证据：桌面1440×900、窄屏390×844的样例布局已检查；原生PDF实际渲染未单独证实，不等同真实外部模型效果验收。
- 保留限制：意外纯预览计算异常当前仅日志/计数，无持久化document.error_code端口；真实外部服务与线上CI未运行。
- 交付状态：代码与验收记录已本地提交；未推送、部署或开启生产功能开关。运行说明见 `docs/operations/08-api-ui-and-local-run.md`，规范见 `spec/README.md`。
