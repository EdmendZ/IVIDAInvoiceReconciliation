# 发票工作台开发记录

## 用户原话

“修正 拒绝问题复杂化 继续开发。”

“继续以合适的模型继续任务 避免使用superpower开发 节省token。”

## 已确认执行方式

在原项目和现有环境直接开发，保留每任务独立上下文、文件范围检查和必要测试。不使用 Superpowers 开发。后续任务按复杂度选择模型，常规开发采用 GPT-5.6 Sol，精简交接和日志输出；已完成上下文不复用。

## 实际进度

T00 检查基座、T01 数据接口、T02 确定性匹配与比对、T03 原子存储均已验收。T03 的 57 项 PostgreSQL/契约测试无跳过；协调者另行复核了事务回滚、并发抢占、同请求重放。旧业务回归和补齐的数据字典检查通过。独立测试库迁移前后，旧版本和对账记录校验值完全一致。

Spec 当前 IR-SIMPLE-1.0.3：补齐历史导出编号、提前同步数据库字典，并给后续集成任务补上已有 CI 的修改范围。均为原需求的实现遗漏修正，不加入新业务。

接下来开发后台自动同步和预览、服务/API、中文工作台；新工作台尚未全量完成或部署。商业合同不在范围内。


## T04—T08 验收更新

- 用户原话：继续以合适的模型继续任务，避免使用 superpower 开发，节省 token。
- 已执行：全程未使用 Superpowers；T04/T05/T06/T08 主实现使用 Sol，T07 使用 Luna。T08 Sol 因额度中断，保留改动，由全新 Luna 上下文完成收尾。
- 已验收：T04 自动 Worker（18 测试）；T05 用例/上传/CSV（13 测试）；T06 HTTP 与旧写入口限制（46 指定＋59 回归）；T07 前端类型/客户端（8 测试）；T08 中文工作台/单据详情/历史只读（54 前端测试、build、范围检查通过）。
- T08 主会话复核修正：自动提取完成填充未修改字段、后到收货单持续轮询、保留未保存输入、预览变化清除确认勾选、切换单据忽略旧响应。
- 主会话已用样例检查 1440×900 与 390×844 页面；长英文、多张 RN、缺价格与差异确认按钮无横向遮挡。属于样例视觉证据，不代表真实数据库端到端或真实 TapTouch 接入。
- 已知限制：意外纯预览计算异常由 Worker 记录日志/计数；当前端口没有将此类异常持久化到 document.error_code 的操作。
- 下一步 T09：独立 PostgreSQL＋API＋Worker 集成验收、文档/CI同步；随后 T10/T11。未部署，未开启生产功能。
